<?xml version='1.0' encoding='utf-8'?>

<!-- The contents of this file will be loaded for Calvalus. -->

<Context>
    <!--
        The "Parameter" elements will be presented to the application when
        requested by calling ServletContext.getInitParameter().
        todo - check why IDEA's GWT support does not recognize context.xml,
               init-parameters are currently read from web.xml (but that may
               be not ok for the Hadoop parameters) (nf)
        note: since Tomcat 7, context.xml is not automatically copied to
              <tomcat7>/conf/<engine>/<host>/calvalus.xml   (nf)
    -->

    <!--
    <Parameter name="calvalus.portal.backendService.class"
               value="com.bc.calvalus.portal.server.hadoop.HadoopBackendService"
               override="true"/>

    <Parameter name="calvalus.hadoop.fs.default.name"
               value="hdfs://cvmaster00:9000"
            override="true"/>

    <Parameter name="calvalus.hadoop.mapred.job.tracker"
               value="cvmaster00:9001"
            override="true"/>
    -->
</Context>